<UserControl x:Class="BcContainerLauncher.App.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BcContainerLauncher.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:LogViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Kopieren" Padding="10,4" Command="{Binding CopyCommand}" Margin="0,0,6,0" />
            <Button Content="Speichern als…" Padding="10,4" Command="{Binding SaveCommand}" Margin="0,0,6,0" />
            <Button Content="Leeren" Padding="10,4" Command="{Binding ClearCommand}" Margin="0,0,12,0" />
            <CheckBox Content="Auto-Scroll" IsChecked="{Binding AutoScroll}" VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#FFFFFF"
                BorderBrush="{StaticResource PanelBorder}"
                BorderThickness="1">
            <ListBox x:Name="LogList"
                     ItemsSource="{Binding Entries}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss.fff}}"
                                       Foreground="#888"
                                       FontFamily="{StaticResource MonoFont}"
                                       Margin="0,0,8,0" />
                            <TextBlock Text="{Binding Level}"
                                       Width="80"
                                       FontWeight="SemiBold"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToBrush}}"
                                       FontFamily="{StaticResource MonoFont}" />
                            <TextBlock Text="{Binding Message}"
                                       Foreground="{Binding Level, Converter={StaticResource LevelToBrush}}"
                                       FontFamily="{StaticResource MonoFont}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</UserControl>
